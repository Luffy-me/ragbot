# Frontend

Next.js App Router UI for University Knowledge AI.

```bash
npm install
NEXT_PUBLIC_API_URL=http://localhost:8000 npm run dev
```

Pages:

- `/login` — student/admin sign-in
- `/chat` — streaming RAG chat with citations
- `/admin` — PDF upload, delete, re-index
- `/settings` — theme + account
